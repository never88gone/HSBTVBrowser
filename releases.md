#### 目前已知的几个问题，暂时不知道怎么解决（如果有了解的朋友也可以给点思路 😁）

1. 爱奇艺、抖音直播、咪咕视频等等视频的播放，使用mse播放的，目前这个在tvos被屏蔽了，短期无法解决。
2. 如果页面是iframe内嵌的，有些内嵌的iframe无法操作。
3. 有些网页的按钮事件不一样，需要用户手动设置为触控模式才能点击。
4. 页面正在播放视频的时候，playpause按键被系统拦截了，需要先暂停视频。

#### 2026-03-09

1. 网页页面可以在设置->浏览器->浏览器页面设置->主题颜色 可以切换app的主题色
2.  修复抖音测试页面的部分bug

#### 2026-03-03

1. 网页页面可以在设置->浏览器->浏览器页面设置->显示内存监控 开启内存监控，方便在网页上显示内存的使用情况
2. 内存使用情况文案调整，估计当前网页的内存占用情况
3. 修复了 抖音(测试)页面的一些bug
4. 网页移动光标的时候，点击模式与触控模式做了融合，避免部分浮层不能弹出。

#### 2026-02-27

1. 二维码扫码做一些兼容，避免跟壳子的二维码端口重复，导致服务启动失败。
2. 首页的工具单独写了一个抖音测试入口，这个入口去掉了鼠标，通过遥控器控制焦点，目前应该还有很多bug，还需要进一步完善，因为是注入js到网页中，需要网页加载完成才能操作。
3. 独立的抖音页面操作说明:<br>
    如果没有登录，应该会弹出登录提示框，上面有二维码，手机扫描二维码登录。<br>
    登录完成后会刷新页面，并且自动跳转到视频页面。<br>
    默认状态下，遥控器上下 箭头控制切换视频，左右按钮控制视频进度，playpuse按钮控制视频播放，长按playpuse唤起播放器。<br> 
    双击select区域进入操作状态，不控制遥控器后5s回到默认状态<br>
    操作状态下，通过左右按钮来切换按钮，点击select按钮控制，目前可以操作的按钮包含：连播、清屏、清晰度、倍速、声音、全屏、点赞、收藏、弹幕<br>
    
#### 2026-02-09

1. 首页的配置可以自定义了，用户到设置->浏览器->浏览器数据管理->上传主页JOSN，可以手机扫码上传json文件。
2. 默认内置了一个json文件，如果想还原默认的首页，可以在浏览器数据管理中清除首页。
3. 手机扫码上传文件的端口号可以手动修改，避免与其他服务器的端口号冲突，目前默认端口号是9961，设置->浏览器->浏览器数据管理->服务器端口。

#### 2026-02-06

1. 首页样式做了一些调整，增加了首页推荐网页，之前首页内容调整为自定义分组。
2. 首页增加了一些推荐的网页，目前测试常用的网站包含 bilibili、腾讯视频、优酷、芒果TV、YouTube、TikTok、QQ音乐、网易云音乐、酷狗音乐、Twitch 都可以正常使用，网友推荐的 cibnabg 分辨率调整为手机模式。
3. 芒果TV默认的配置调整为全屏展示，网友推荐的 cibnabg 默认配置为手机分辨率，都需要用户恢复默认配置才能生效。

#### 2026-02-03

1. 内置 TikTok 的配置，之前的默认配置会导致 TikTok 进入后不能操作，TikTok 可以选择扫码登录，上下切换视频，需要用户自备梯子。
2. 抖音模拟设备只要不设置为 iPad mini，iPhone，就可以显示登录二维码，用户可以扫码登录了。目前的抖音的默认模拟设备是 iPad mini，需要用户手动切换。

#### 2026-01-29

1. 完善内置的 js 文件，可以通过 js 文件设置抖音的分辨率，自动连播。
2. 登录有些受到安全限制。

#### 2026-01-27

浏览器设置里面增加了中文、英文、土耳其语言的切换。

#### 2026-01-22

如果当前光标指示位置可以点击，光标增加一个小角标。

#### 2026-01-21

1. 修复一些内存回收不及时的问题。
2. 当内存快不足了，或者已经不足了，添加逻辑释放浏览器的内存。

#### 2026-01-20  主要是修改昨天版本发现的问题

1. 浏览器操作设置中，调整轻触开关调整，现在轻触 select 区域，通过轻触的时间与移动距离识别。
2. 新增了两个默认的 js 文件，douyin_fullscreen_delay.js 会增加容错机制，页面加载结束后调用这个 js 文件，会做多次尝试调用抖音的全屏，之前 douyin_fullscreen.js 文件调用的时候页面可能还没显示完，导致全屏不成功。

#### 2026-01-19

1. 浏览器针对域名可以设置自定义的全屏事件，加载结束的事件，可以执行 js 文件。
2. 高级菜单里面添加自定义的按钮，可以指定 js 文件执行。
3. 浏览器操作设置里面增加脚本管理，可以通过扫码上传 js 文件。目前只内置了两个，抖音、腾讯视频的全屏，后续再尝试提供其他操作的 js 文件。
4. 浏览器页面设置中可以隐藏提示词。
5. 浏览器操作设置中，可以设置点击是通过轻触触控区域，还是点击触控区域（目前手势有冲突，轻触模式有问题）。
6. 浏览器操作设置中，可以设置光标的灵敏度，灵敏度默认是 1，如果设置为 2，移动距离就只有之前的 0.5 倍。
7. 浏览器操作设置中，可以开启使用 Apple Watch 操作浏览器，目前支持通过翻腕滚动到下一个视频（Watch 应用待上传 TF 版本，用户先不要打开）。

#### 2026-01-05

1. 清除缓存的时候不再删除 cookies 信息。
2. 修复首页数据不能往下滚动的 bug。
3. 尝试修复首页滚动后震动问题、设置页面回退闪烁问题（本地一直没复现，不太确定是否改好了）。

#### 2025-12-27

1. 设置页面分组调整。
2. 移除设置中的分辨率、模拟设备、WebKit、页面自适应的设置，移动到 3 的默认中。
3. 设置中增加特定域名行为，可以针对不同的域名配置不同的分辨率、模拟设备、WebKit、页面自适应，目前先内置了几个网站，用户也可以自己设置常用的，如果没有匹配到域名的使用默认设置。
4. 退出、测试页面不再添加到历史记录中。
5. 监控日志页面增加折线图的展示，可以更加方便观察内存的使用情况。
6. 样式继续优化。
7. 首页的首次使用的提醒样式调整，说明了 App 目前存在的一些问题。
8. 优化了 js 的调用事件，避免报错，减少内存的使用。

#### 2025-12-26

1. 设置中增加监控与日志打印，通过这两项来观察浏览器的内存占用情况。
2. 首页首次使用、浏览器设置中增加注意事项。
3. 浏览器使用共享线程池，先尝试下使用效果，看看对白屏有没有优化。

#### 2025-12-25

1. 浏览器设置页面增加光标的控制，可以设置光标自动隐藏，以及自动隐藏的时间，默认是不自动隐藏的。
2. 使用 AI 美化了页面。
3. 首页如果没有数据，增加空态提醒。
4. Switch Pro 手柄已知问题修复。
5. 帮助页面调整。

#### 2025-12-24

1. 浏览器设置中可以设置网页分辨率，目前分为全屏分辨率、手机分辨率、iPad 分辨率，配合模拟设备使用，部分网页手机端可以播放视频了。
2. 高级菜单添加模拟设备、网页分辨率，可以只修改当前页面，不全局修改值。
3. 高级菜单中部分按钮次序调整。

#### 2025-12-23

1. 浏览器的帮助中心中添加图片说明帮助。
2. 添加手柄的支持（目前手柄的使用测试有限，待稳定后再添加帮助说明）。
3. 网页上添加时间显示，默认是关闭的，可以到设置里面打开，添加了时钟设置页面。
4. 退出网页的页面如果时间过长会显示文案，之前是空白白色页面。
5. 修复部分闪退问题，修复内存泄露问题。
6. 浏览器首页不再显示版本信息，添加到关于页面。
7. 投屏首页可以向右滑动弹出二维码，避免旧版遥控器无法使用。
8. 投屏版本浏览器模式切换后，页面弹出提示，并且刷新二维码。

#### 2025-12-19 (浏览器)

1. 设置页面继续分组展示，与投屏版本公用页面。

#### 2025-12-17

1. 设置页面设置条目太多了，分成多个页面。
2. 高级菜单中的全屏按钮可以控制腾讯视频全屏(设置中模拟设备不能是 iPad mini、iPhone)。
3. 优化了高级菜单中的放大缩小字体了，放大字体后之前有些不容易点击拖动的按钮也方便操作了(目前测试抖音、腾讯视频的弹幕可以通过这种方式调整)。

#### 2025-12-15

1. 几种模式的鼠标样式做了调整，点击的位置偏差问题调整。
2. 网页上的视频全屏做了拦截，唤起 App 的播放器，不再唤起浏览器自带的播放器，之前全屏后只能强杀 App。
3. 修复设置中的阻止屏保、页面自适应的缓存问题。
4. 设置中增加显示模式的设置，部分页面能显示暗夜模式，还未完成。
5. 无痕模式调整，只是不保存记录到历史记录，其他缓存模式与普通模式一致。
6. 关于中 GitHub 地址增加二维码的显示。

#### 2025-12-12

1. 鼠标按钮图标优化，点击位置偏差修复。
2. 删除缓存不再删除设置。
3. 网页上的提示文案样式调整。

#### 2025-12-11

1. 设置页面之前只能设置手机模式、桌面模式，调整为通过 UserAgent 模拟不同的设备，目前默认模拟 iPad mini（优化解决网页白屏问题）。
2. 设置页面新增一个选项可以阻止 Apple TV 的屏幕保护，默认是关闭的。
3. 设置页面新增一个选项，网页自动自适应，设置部分 UserAgent 后，有些页面显示不太适配 TV 上的显示，默认是关闭的。
4. 设置页面新增还原配置的按钮，可以一键还原设置（这个不会清除网页的内容）。
5. 设置页面样式调整。

#### 2025-12-08

1. 设置中添加了 Telegram 的频道二维码，方便用户反馈。
2. 高级菜单中添加了退出浏览器，可以直接退出当前页面。
3. 抖音短剧的页面现在可以点击了。
4. 系统播放器、VLCPlayer 可以唤起播放抖音短剧。
5. FSPlayer 播放器不能控制速度，不能看抖音短剧。

#### 2025-12-05

1. 高级菜单中增加了全屏、退出全屏按钮，目前只有抖音能用。
2. 抖音网页打开播放器后播放进度的 bug 修复。
3. 系统播放器现在可以设置播放速率。
4. 历史记录不在反复重复添加。
5. 桌面图标稍微调整了一下。
6. 首页弹窗提醒与帮助信息更新。

#### 2025-12-04 （这两天逻辑改动有点多，可能导致新的问题）

1. 新增拖动模式，有些网页验证码需要拖动，拖动模式下长按 Select 按钮选择验证码，可以拖动验证码。
2. 网页新增 hover 事件，抖音有些按钮需要鼠标悬停后才能显示，之前无法操作，现在可以操作了。
3. 网页页面默认左右滑动会显示视频控制器，现在默认值调整为关闭，需要到设置中打开。
4. 网页上获取视频进度的时机做了优化，少了一个定时器，卡顿好一些了。
5. 新增百度搜索，之前百度页面打开会触发验证码，现在不会了。
6. 网页内容是使用的 iframe，iframe 的内容之前是无法点击了，现在增加了提示文案，在点击模式下，可以长按 Select 按钮，单独打开 iframe 页面，具体操作可以到新开页面中操作。
7. 播放器页面新增按钮可以控制播放进度。

#### 2025-12-01

1. 输入页面长按 Play/Pause 按钮也可以完成输入。
2. 高级菜单中的返回首页逻辑调整，现在重新创建网页，之前是加载首页，导致页面可以不停的返回。
3. 首页的搜索功能，不输入文字也可以跳转页面。
4. 收藏与历史页面如果是空白的时候增加了空白的文字提示。
5. 鼠标的 hover 提示文案可以在设置中设置显示位置，设置了一点透明度。
6. 提示当前是滚轮模式的提示图标可以在设置中设置显示位置，设置了一点透明度。

#### 2025-11-28

1. 关于页面重新调整。
2. 设置页面左边的空间利用起来，可以显示当前选中信息的描述。
3. 设置页面新增无痕模式，看的网页不会添加到历史中。
4. 设置页面增加两个选项，可以控制播放视频的网站是否显示视频控制器。
   左右滑动的手势与滑动鼠标的有点冲突，用户自己决定开关。
5. 网页地址在部分网站一直不消失的 bug 修复。

#### 2025-11-27 （测试不够，不影响功能使用，没发 TF 版本）

1. 统一网页、视频的播放器控制组件。
2. 首页顶部的 tab 调整大一点。
3. 操作提示文案，之前前面有一段空白，现在加上图标。
4. 页面自适应、关闭页面自适应、设置字体大小、关闭设置字体大小的函数用 AI 生成，兼容性更强了。

#### 2025-11-26

1. 首页加入了 Google 搜索、Bing 搜索。
2. 首页移除掉了默认的推荐网页--百度(百度使用需要验证码登录，在桌面模式下会反复跳转)，如果之前使用过会缓存下来，不会自动移除。
3. 网页进入会在底部显示当前的 URL 地址，直到页面加载结束后 5s 消失。
4. 修复抖音上获取视频播放地址不准确的问题。
5. 去掉高级菜单中的全屏、退出全屏功能，该功能会导致 App 异常。
6. 网页上带有视频，长按 Play/Pause 按钮就会打开播放器，之前抬起按钮。
7. 网页上带有视频，修复 Play/Pause 点击后视频不能播放的问题。
8. 首页长按 Select 按钮弹出删除提示会显示需要删除信息。

#### 2025-11-25

1. 打开播放器时带上当前播放时间。
2. 非系统播放器添加播放进度栏控制展示。
3. 网页的视频播放的进度控制器滑动灵敏度调整。

#### 2025-11-24

1. 发布最近两个月的代码，之前一直没提交到 TestFlight 上。
2. 最主要的是修复闪退问题，之前有些在线视频获取播放时长的时候会闪退。
3. 腾讯视频使用播放器获取的地址错误，增加了兼容控制。
4. 返回按钮的逻辑调整。
5. 从 AppDelegate 切换为 SceneDelegate。
6. 退出 App 后，再进入缓存导致的数据异常闪退。
7. 网页播放视频时，可以通过遥控器的左右按钮，左右扫动控制视频播放的进度。
8. 箭头使用图片，不再是纯色的图标，在不同的背景下方便区分。
9. 退出页面音频关闭。
10. 高级菜单的样式调整。
11. 一些内存溢出问题。
12. 输入框无法输入的问题、输入页面重写。

#### 2025-11-23

1. 更新说明文件，之前为了 Apple 审核，GitHub 上的描述调整为投屏的。
2. 暂时不考虑上架 App Store，浏览器的首页继续使用之前的一些网站。